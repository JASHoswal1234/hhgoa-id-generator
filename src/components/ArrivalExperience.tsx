import { GoaEnvironment } from './GoaEnvironment'

/**
 * Phase 1: HH Goa Immersive Arrival Experience
 * 
 * ONE WORLD. ONE WINDOW. TWO GENERATOR MODES.
 * 
 * The Goan window is the scene. The generators live inside the window.
 * The surrounding illustrations are physical objects in that scene.
 * 
 * This is NOT:
 * - Multiple separate sections
 * - Floating PNGs around a UI card
 * - A landing page with decorations
 * 
 * This IS:
 * - A physical Goan environment
 * - Where the window is the central architectural object
 * - And the interactive experience happens THROUGH the window
 */
export function ArrivalExperience() {
  return <GoaEnvironment />
}
